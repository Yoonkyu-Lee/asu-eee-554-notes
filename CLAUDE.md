# EEE 554 학습 노트 프로젝트

## 이 저장소가 하는 일

ASU EEE 554 (Probability and Random Processes, Michelusi, Fall 2026) 강의 슬라이드를
한 장씩 분해해서, 시각 자료와 예제와 반례가 풍부한 개인 학습 노트 HTML로 재구성한다.
노트 옆에 원본 슬라이드 PDF를 띄우고 스크롤을 동기화해서, 지금 읽는 문단이
슬라이드 몇 쪽에서 나온 것인지 바로 보이게 한다.
GitHub Pages로 배포된다: https://yoonkyu-lee.github.io/asu-eee-554-notes/

사용자(YK)는 UIUC Computer Engineering 학부를 졸업하고 ASU에서 MS 중인 한국인 학생이다.
디지털 설계 / 반도체 배경이 있고, ECE 385, 391, 408, 411, 444를 들었다.
비유는 환영하지만 매번 학교 수업에 억지로 연결할 필요는 없다.

## 경로

| 용도 | 경로 |
|---|---|
| 저장소 | `D:\Engineering\asu-eee-554-notes` |
| 슬라이드 원본 (읽기 전용) | `D:\Library\01 Immigration Documents\02 ASU\FA26\EEE 554` |

Drive 미러 폴더는 절대 수정하지 않는다. 읽기만 한다.

**커밋하는 것**: 모듈에 대응되는 강의 슬라이드 PDF만. `slides/`에 사본을 두고 커밋한다.
사이트에 PDF 리더를 붙여 노트와 슬라이드를 나란히 보기 위한 것이다.

**커밋하지 않는 것**: 교재(Kay, 57MB), `randomprocJuly14.pdf`, 강의계획서, `HW*.pdf`.
용량 때문이기도 하고, 리더가 필요로 하지 않기 때문이기도 하다.
HW는 노트를 쓸 때 읽기만 하고 커밋하지 않는다.

## 파일 명명

```
index.html                          허브 (모듈 목록)
L01-set-theory.html                 Module 1
L02-probability-space.html          Module 2
L03-conditional-probability.html    Module 3
L{NN}-{kebab-case-영문주제}.html

HW1-sets-and-probability-spaces.html   HW1 공략 (아래 참조)
HW{N}-{kebab-case-과제제목}.html

slides/L01-set-theory.pdf           Module 1 슬라이드 (노트와 같은 stem)
slides/L02-probability-space.pdf
slides/L03-conditional-probability.pdf

reader.js  reader.css               슬라이드 리더 (모든 노트가 공유)
vendor/pdf.js/                      pdf.js 런타임 (아래 참조)
```

슬라이드 `03 ConditionalProbability.pdf` → `L03-conditional-probability.html`.
번호는 슬라이드 파일 번호를 따른다.

**슬라이드 사본은 노트와 stem을 맞춰서 이름을 바꾼다.** Drive 원본은 공백이 들어간
`03 ConditionalProbability.pdf`지만 URL에 쓰기 나쁘다. `slides/`에 복사하면서
`L03-conditional-probability.pdf`로 바꾼다. 그러면 노트 파일명에서 슬라이드 경로가
바로 유도되므로 리더가 하드코딩된 매핑 표를 들 필요가 없다.

## 언어와 문체

- **모든 본문은 한국어.** 반말체, 설명하듯이. "~야", "~거야", "~돼".
- **em dash(—) 금지.** 콜론, 쉼표, 마침표를 쓴다. (SVG 안이나 구분선 용도는 예외)
- 수학 용어와 고유명사는 영어 유지: sample space, σ-algebra, partition, De Morgan, Kolmogorov.
- 과장 금지. "놀랍게도", "사실은" 같은 수사보다 정확한 서술.
- 사용자는 근거 없는 주장을 바로 잡아낸다. 슬라이드에 없는 내용을 추가할 때는
  그게 슬라이드 밖 보충이라는 걸 문맥으로 알 수 있게 쓴다.

## 모듈 노트의 5단 구조

각 개념 섹션은 이 순서를 반복한다.

1. **목차와 요약** — 이 모듈이 무슨 일을 하는지, 새 용어가 뭔지, 나중에 어디서 쓰이는지.
   섹션 00에 배치. "새로 나오는 단어들" 목록과 그중 뭐가 제일 중요한지 명시.
2. **개념 정리** — 슬라이드의 정의를 그대로. 여기까지는 원본과 크게 다르지 않다.
   `.def` 블록 사용. Definition 번호가 슬라이드에 있으면 그대로 인용.
3. **비주얼** — 수식만 있는 정의에 붙이는 SVG. **이게 이 프로젝트의 존재 이유다.**
   교수가 벤 다이어그램을 그릴 때 사용자가 이해하기 시작한다는 게 출발점이었다.
4. **예제** — 시험 대비용. `<details>`로 풀이를 접어둔다. 먼저 풀어보게 하는 것이 목적.
5. **반례와 함정** — 개념이 성립하지 **않는** 경우. `.trap` 블록.
   오해하기 쉬운 지점을 미리 잡아낸다. 사용자가 가장 가치 있게 여기는 부분.

마지막 섹션은 항상 **정리 & 다음 모듈**: 한 장 요약 표 + HW 대응표 + 다음 모듈 예고.

## 함정 섹션 작성 원칙

이게 노트의 차별점이므로 대충 쓰지 않는다.

- 시험에서 실제로 틀릴 만한 것 위주. "이론적으로 가능한 오해"가 아니라 "흔한 실수".
- **구체적인 반례를 숫자로 제시한다.** "성립하지 않는다"로 끝내지 말고
  `Ω={1,2}, A={1}, B={2}` 처럼 반례를 만들어 보여준다.
- 표기 레벨 혼동(`∈` vs `⊆`, 원소 vs 집합)은 반복해서 짚는다. 계속 틀리는 지점이다.
- 유한에서 참인 것이 무한에서 깨지는 경우를 항상 확인한다.
- "왜 이걸 배우는지"를 함정 섹션 마지막에 넣으면 효과가 좋다.

## 모듈 간 연결

**필수 작업이다.** 각 모듈에서 이전 모듈의 개념이 실제로 어디서 일하는지 명시적으로 짚는다.
예: Module 2의 σ-algebra 필요성은 Module 1의 가산/비가산 구분이 전제다.
`.callout` 블록으로 "Module N의 X가 여기서 일한다" 형태로 쓴다.
마지막 정리 섹션에 이전 모듈 ↔ 현재 모듈 대응표를 넣으면 좋다.

## 과제 처리 원칙

`HW*.pdf`를 읽고 해당 모듈 범위의 문제를 파악한다.

- **개념/증명 문제** (예: "공리에서 유도하라", "σ-algebra인지 검증하라"):
  방법이 핵심이므로 같은 유형을 **풀이까지 전부** 보여준다.
- **순수 계산 문제** (예: "c를 구하라", "P(A)를 계산하라"):
  절차는 같고 **숫자를 바꿔서** 예제로 낸다. 그리고 "HW는 숫자가 다르니 직접 해봐"라고 명시.
- 문제지에 "노트 보지 말고 풀어라"라고 쓰인 문제는 그 지시를 노트에서 다시 강조한다.
- 마감일이 있으면 헤더 chip과 정리 섹션에 표시한다.

### 공략 페이지 (숙제 → 개념 방향)

노트 안의 HW 언급은 **개념 → 숙제** 방향이다. 읽다가 "아 이게 HW1 P4구나"를 알려준다.
공부할 때는 이게 맞지만, **숙제를 펴놓고 앉았을 때는 방향이 반대다.**
"P5 풀려면 뭘 봐야 하지"에 답하는 곳이 따로 필요하다.

**별도 페이지로 만든다.** 인라인으로는 구조적으로 안 되기 때문이다.
HW1이 Module 1(P1-3)과 Module 2(P4-8)에 걸쳐 있어서, 각 모듈의 대응표는
자기 범위만 안다. 과제 전체를 한눈에 보는 곳이 어느 노트에도 생길 수 없다.

- 파일명은 `HW{N}-{과제 제목 kebab-case}.html`. 문제지 제목을 그대로 쓴다.
- 문제 번호순으로 정렬한다. 개념순이 아니다. 진입점이 문제 번호이기 때문이다.
- 문제마다 네 가지: **필요한 개념 → 노트 링크 / 첫 수 / 빠뜨리기 쉬운 것**.
  섹션 번호는 `.sec-num`에 `P5` 형태로 넣는다. `verify.mjs`가 이걸 읽는다.
- **답은 쓰지 않는다.** 계산 결과를 적으면 공략이 아니라 해답지가 된다.
- **문제 원문을 그대로 싣지 않는다.** repo가 Public이라 인터넷에 게시하는 것이고,
  `HW*.pdf` 자체를 커밋 안 하기로 한 것과 같은 이유다. "P3: 패킷 2회 전송,
  표본공간과 분할" 정도의 서술로 충분하다. 채점 방침처럼 인용이 필요한 대목만
  짧게 따온다.
- **"노트 보지 말고 풀어라" 문제는 접어둔다.** 다른 항목과 똑같이 접근법을 펼쳐두면
  그 문제가 재려던 것을 파괴한다. `<details>`로 접고 왜 접었는지 위에 적는다.
  HW1 P8이 이 경우다.
- 제출물에 회고(Reflection)가 있으면 같이 싣는다. 개념 노트에는 들어갈 자리가 없고,
  문제만 풀고 빠뜨리기 딱 좋다.
- 슬라이드를 다루지 않으므로 `data-slide`를 붙이지 않는다. 앵커가 없으면
  리더가 알아서 안 뜬다. `reader.js`도 넣지 않는다.

**모듈 노트 끝의 HW 대응표는 그대로 둔다.** 모듈 범위에서 "방금 뭘 할 수 있게
됐나"를 읽는 것이라 역할이 다르다. 대신 양쪽이 갈라지지 않게 `verify.mjs`가
문제 번호를 대조한다 ([검증](#검증) 참조).

## HTML 기술 규격

### 자체완결형

**CDN 금지.** 네트워크에서 뭔가를 받아오는 코드를 쓰지 않는다.
노트 본문의 CSS, SVG, JS는 전부 그 HTML 파일 하나에 인라인한다.
오프라인에서 열어도 노트는 완전히 동작해야 한다.

예외는 PDF 리더 하나뿐이다. `vendor/pdf.js/`와 `slides/*.pdf`는 repo 안에 있는
로컬 파일이므로 "외부 의존성 없음"은 유지된다. 다만 **리더는 없어도 되는 부가 기능이다.**
`vendor/`나 `slides/`가 없거나 로드에 실패해도 노트 본문은 그대로 읽혀야 한다.
리더를 노트 읽기의 전제 조건으로 만들지 않는다.

### 수학 표기

KaTeX나 MathJax를 쓰지 않는다. **유니코드 + 세리프 폰트**로 처리한다.

```html
<span class="m"><i>A</i> ∩ <i>B</i><sup>c</sup></span>   <!-- 인라인 -->
<div class="eq"><i>P</i>(<i>A</i><sup>c</sup>) = 1 − <i>P</i>(<i>A</i>)</div>  <!-- 디스플레이 -->
```

- `.m` / `.eq` = 세리프, upright. 변수 letter만 `<i>`로 감싼다.
- 기호는 유니코드 직접: `∩ ∪ ⊆ ∈ ∉ ∅ Ω ω ℱ ℝ ℕ ℤ ℚ ℬ σ ⋃ ⋂ ∑ ∫ ≤ ≥ ≠ ⟹ ⟺ ∀ ∞ −`
- 마이너스는 하이픈이 아니라 `−` (U+2212).

### 기호 뜯어보기 (`.nota`)

사용자는 수학 표기 관습을 배운 적이 없다. `⋃<sub>i=1</sub><sup>∞</sup> A<sub>i</sub> ∈ ℱ`
같은 줄은 개념이 어려운 게 아니라 **읽는 법을 몰라서** 막힌다. 그 관습만 따로 푼다.

```html
<details class="nota" open>
  <summary><span lang="ko">기호 뜯어보기 · 조건 ③</span><span lang="en">Unpacking the notation · ③</span></summary>
  <div class="body">
    <div lang="ko">…</div>
    <div lang="en">…</div>
  </div>
</details>
```

- **기본은 펼쳐둔다(`open`).** 모르는 사람에게 안 보이면 있으나 마나다.
  대신 접을 수 있다. 알고 나면 매번 지나가야 하는 장애물이 되면 안 된다.
- 정의 블록(`.def`) **바로 다음**에 둔다. 그 줄을 못 읽으면 아래가 다 막히기 때문이다.
- 부위별 표(`.parts`)로 쪼갠다. **기호 하나 = 한 줄.** 그리고 펼친 형태를 `.eq`로 보여준다.
  `⋃<sub>i=1</sub><sup>∞</sup> A<sub>i</sub> = A<sub>1</sub> ∪ A<sub>2</sub> ∪ ⋯` 처럼.
- 아는 것에 얹는다. 큰 기호는 `∑`와 골격이 같다고 하면 한 번에 끝난다.
- 레벨 혼동(`∈` vs `⊆`, 원소 vs 집합)은 여기서도 짚는다. 계속 틀리는 지점이다.
- 새 모듈에서도 **정의에 처음 보는 표기가 있으면 같이 넣는다.** 나중에 몰아서 하지 않는다.
- 같은 기호를 두 번 풀지 않는다. 처음 나오는 곳에서 풀고, 뒤에서는 그리로 링크한다.

### 디자인 토큰 (모든 파일 공통)

색마다 **RGB 성분을 따로 둔다.** 도해에서 같은 색을 여러 농도로 쓰기 때문이다.
이렇게 해두면 다크모드에서 RGB 하나만 바꿔도 모든 농도 변형이 같이 따라온다.

```css
--blue-rgb:18,80,196;
--blue:rgb(var(--blue-rgb));
/* 농도 변형은 rgba(var(--blue-rgb),.16) 형태로 쓴다 */
```

`index.html`, `L01`, `L02`의 `:root` 블록은 **한 글자도 다르지 않게 동일**하다.
새 파일도 그대로 복사한다. 여기 없는 변수를 새로 만들지 않는다.

**SVG 안에서도 CSS 변수를 쓴다.** `fill="var(--blue)"`는 프레젠테이션 속성에서도
정상 동작한다. (예전에 "SVG는 변수가 안 먹으니 리터럴을 쓰라"고 적어뒀던 건 틀린 내용이었다.
그 때문에 색이 635곳에 하드코딩돼 있었고 다크모드를 넣으려면 전부 고쳐야 했다.)

**본문 링크에는 반드시 색 규칙을 준다.** 규칙이 없으면 브라우저 기본 남색이 나오고,
다크모드에서 배경에 묻는다(실측 대비 1.94:1). 모듈 노트는 본문 링크가 0개라
이 결함이 오래 드러나지 않았고, 링크가 본체인 공략 페이지에서 바로 터졌다.

```css
main a{color:var(--blue);text-decoration:none;border-bottom:1px solid rgba(var(--blue-rgb),.4)}
```

**색을 하드코딩하지 않는다.** 하드코딩하면 다크모드에서 그 부분만 안 바뀐다.
`verify.mjs`가 소스에서 리터럴 색을 잡아 실패시킨다.

**예외는 `<mask>` 안뿐이다.** 마스크의 `#000`/`#fff`는 색이 아니라 알파 채널이라
테마를 입히면 도형이 깨진다. 검사도 마스크 안은 건너뛴다.

**의미 고정 색상.** 모든 다이어그램에서 일관되게 지킨다.

| 대상 | 색 |
|---|---|
| 집합 A / 첫 번째 대상 | `--blue` |
| 집합 B / 두 번째 대상 | `--pink` |
| A ∩ B / 강조 / 선택됨 | `--violet`, `--sel` |
| 집합 C / 세 번째 대상 / 정답 | `--green` |
| 함정, 경고, 틀린 것 | `--amber` |

세 번 이상 반복되면 독자가 색만 보고 의미를 알게 된다. 이게 이 노트의 시그니처다.

**규칙을 어기면 인접한 도해끼리 색이 충돌한다.** 포함배제 도해가 <span>A ∩ B</span>를
amber로 칠하고 있었는데, 바로 위 도해가 amber를 "틀린 경우"로 쓰고 있어서 같은 색이
한 화면에서 두 가지를 뜻했다. 새 도해를 그릴 때뿐 아니라 **기존 도해를 손볼 때도
색이 규칙에 맞는지 확인한다.** 자동 검사는 색이 토큰인지만 보지 의미까지는 못 본다.

## 다크모드

기본은 OS 설정(`prefers-color-scheme`)을 따르고, 좌하단 버튼으로 덮어쓸 수 있다.

- 다크 팔레트는 `@media (prefers-color-scheme:dark){:root:not([data-theme="light"])}`와
  `:root[data-theme="dark"]` 두 곳에 같은 값을 적는다. 앞은 자동, 뒤는 수동 전환용.
- **의미 고정 색상의 의미는 유지하고 밝기만 올린다.** A는 여전히 파랑, B는 분홍이다.
- 토글 상태는 **저장하지 않는다.** 브라우저 저장소 금지 규칙 때문에 페이지를 옮기면
  다시 OS 설정을 따른다.
- 슬라이드 PDF는 원본 문서라 다크모드에서도 흰 종이 그대로 둔다.
  반전 필터를 씌우면 손글씨와 컬러 도형이 이상해진다.

### 블록 클래스

| 클래스 | 용도 | 시각 |
|---|---|---|
| `.def` | 정의, 정리 | 검은 좌측 바 |
| `.trap` | 함정, 반례 | amber 배경 + 좌측 바 |
| `.ex` | 예제 (내부에 `<details>` 풀이) | 흰 카드, 파란 태그 |
| `.callout` | 연결, 강조 | 상하 실선 |
| `.nota` | 기호 뜯어보기 (기본 펼침) | 점선 테두리 + 회색 배경 |
| `figure` + `figcaption` | SVG 도해 | 흰 카드 |
| `.mini` | 작은 SVG 카드 (grid2/3/4 안에) | 흰 카드 |
| `.play` | 인터랙티브 도구 | 흰 카드 |

기존 `L01`, `L02` 파일의 `<style>` 블록을 그대로 복사해서 시작하고, 필요한 것만 추가한다.

### SVG 규칙

- 항상 `viewBox` 지정, `width`/`height` 속성은 쓰지 않는다 (CSS가 100% 폭 처리).
- `role="img"` + `aria-label` 필수.
- 텍스트는 `.svgtxt`(세리프), `.svglab`(모노, 라벨), `.svgnum`(모노, 숫자) 클래스 사용.
- **크기와 색은 반드시 인라인 `style`로 준다. 프레젠테이션 속성은 클래스에 진다.**
  `.svgtxt{font-size:15px;fill:var(--ink)}`이 있으므로 `font-size="58"`은 그냥 무시되고
  전부 15px로 나온다. 실제로 큰 합집합 기호가 본문 글자만 하게 렌더된 적이 있다.
  `style="font-size:58px"`, `style="fill:var(--ink2)"`로 쓴다. (`fill="var(--blue)"`처럼
  클래스가 `fill`을 안 정하는 경우에만 속성이 통한다.)
- **좌표는 짐작하지 말고 재서 맞춘다.** 글리프 실제 크기는 font-size와 많이 다르다.
  다만 **`getBBox()`는 조상의 `transform`을 반영하지 않는다.** `<g transform="translate(…)">`로
  패널을 늘어놓은 도해에서 `getBBox()`로 재면 모든 패널이 같은 자리에 겹쳐 있는 것처럼
  나온다. 실제로 이걸 모르고 "라벨 4개가 전부 겹쳤다"는 거짓 결과를 한 번 냈다.
  **`transform`이 끼어 있으면 `getBoundingClientRect()`를 쓴다.** 화면 좌표라 전부 반영된다.
- **여러 패널을 가로로 늘어놓을 때는 간격을 먼저 계산한다.** 패널 폭 × 개수가 viewBox를
  넘으면 패널끼리 겹치고, 그 사이에 둘 연산자(`+`, `−`, `=`)가 놓일 자리가 사라져
  옆 패널의 도형 위에 그려진다. 포함배제 도해가 실제로 이랬다.
- 영역 색칠은 `clipPath` + `mask` 조합. 복잡한 path를 손으로 계산하지 않는다.
- `<defs>` 안의 id는 파일 내에서 유일해야 한다. 여러 SVG에 같은 id를 쓰면 렌더링이 깨진다.
- 도형 밖으로 텍스트가 나가지 않게 좌표를 계산한다. **검증 스크립트가 이걸 잡는다.**

### 인터랙티브 요소

모듈마다 하나씩, 그 모듈의 핵심 개념을 손으로 만져볼 수 있는 도구를 만든다.

- L01: 3집합 벤 다이어그램 영역 선택기 (De Morgan, 분배법칙 확인)
- L02: σ-algebra 조건 검사기 (16개 부분집합 토글, 위반 사유 실시간 표시)

원칙: **결과를 보여주는 게 아니라 사용자가 조작해서 발견하게 한다.**
"두 식이 같다"고 말하는 대신, 두 프리셋을 눌러서 같은 그림이 나오는 걸 보게 한다.

브라우저 저장소(localStorage 등) 사용 금지. 모든 상태는 JS 변수로만.

## 슬라이드 리더 (데스크톱 전용)

노트 오른쪽에 강의 슬라이드 PDF를 띄우고, **노트를 스크롤하면 슬라이드가 따라온다.**
목적은 두 가지다. 진도를 따라가는 감각을 주는 것, 그리고 이 문단의 근거가 슬라이드
몇 쪽인지 바로 확인할 수 있게 하는 것.

### 구성

| 파일 | 역할 |
|---|---|
| `reader.js` | 리더 전부. 노트는 `<script src="reader.js" type="module"></script>` 한 줄만 넣는다 |
| `reader.css` | 리더 스타일. `reader.js`가 직접 주입하므로 노트에 `<link>`를 쓰지 않는다 |
| `vendor/pdf.js/` | pdf.js 6.x (Apache-2.0). `npm i -D pdfjs-dist` 후 `build/pdf.min.mjs`와 `build/pdf.worker.min.mjs`를 복사한 것 |

노트 본문은 파일 하나에 인라인하지만 **리더는 공유 파일이다.** 모듈마다 복제하지 않는다.

`vendor/pdf.js/`에 pdf.js를 넣어서 쓴다. CDN에서 불러오지 않는다.
브라우저 내장 PDF 뷰어(`<iframe src="x.pdf#page=N">`)는 **쓰지 않는다.**
현재 스크롤 위치를 읽을 수 없어서 역방향 동기화가 불가능하고 페이지 이동이 깜빡인다.

`file://`로 열면 브라우저가 PDF fetch를 막는다. 리더가 프로토콜을 확인해서
아예 시도하지 않고 안내 문구를 대신 띄운다. 로컬에서 리더까지 보려면 서버로 열어야 한다:
`npx serve .` 또는 `python -m http.server`.

### 앵커 규칙

동기화의 기반은 노트 안에 박아둔 슬라이드 쪽수다. 이건 **노트를 쓸 때 같이 넣는다.**
나중에 몰아서 붙이려 하면 슬라이드를 다시 다 읽어야 한다.

```html
<div class="sec-head" data-slide="14"><span class="sec-num">03</span><h2>σ-algebra</h2></div>
<div class="sec-head" data-slide="26-28">…</div>   <!-- 여러 쪽에 걸친 구간 -->
<h3 data-slide="17">생성된 σ-algebra</h3>          <!-- 섹션보다 잘게 끊고 싶을 때 -->
```

**앵커는 `<section>`이 아니라 `.sec-head`(와 `h3`)에 붙인다.** 위치는 섹션 상단과
같아서 동기화 동작은 다르지 않지만, 이래야 CSS가 `attr(data-slide)`로 출처 라벨을
직접 만들 수 있다. `attr()`은 자기 자신의 속성만 읽으므로 조상에 있으면 못 쓴다.

- `data-slide`는 **1부터 시작하는 PDF 쪽 번호**다. 슬라이드에 인쇄된 번호가 아니다.
  **둘은 자주 어긋난다.** L02는 18쪽이 손으로 쓴 삽입 페이지라 그 뒤로 인쇄 번호가
  PDF 쪽보다 1씩 작다 (PDF 27쪽에 "26"이 찍혀 있다). 반드시 PDF 기준으로 센다.
- 슬라이드 내용을 다루는 모든 섹션의 `.sec-head`에 붙인다. 한 섹션이 여러 장에
  걸치면 `h3`에 더 잘게 붙인다. 섹션 범위가 그 안의 `h3` 범위를 포함하는 건 정상이다.
- **앵커를 붙이면 독자에게 출처 라벨이 자동으로 보인다.** 제목 오른쪽에 옅은 선과
  `슬라이드 12-17쪽`이 뜬다. CSS가 `data-slide` 값에서 만들기 때문에 라벨과 앵커가
  어긋날 수 없고, 리더가 꺼져 있거나 모바일이어도 보인다.
  라벨을 손으로 적지 않는다. 적으면 반드시 언젠가 어긋난다.
- 슬라이드에 없는 내용(보충 설명, 반례, 함정, 인터랙티브 도구)에는 **붙이지 않는다.**
  앵커가 없는 구간은 리더가 직전 앵커를 유지한다. 이게 "이건 슬라이드 밖 내용"이라는
  신호도 된다.
- **한 슬라이드는 그것을 실제로 다루는 곳 한 군데에만 앵커를 단다.**
  다른 데서 앞질러 인용하는 건 본문 문장으로만 쓰고 앵커를 붙이지 않는다.
  범위가 겹치면 역방향 동기화(PDF → 노트)가 어디로 갈지 정해지지 않는다.
  `verify.mjs`가 겹침을 잡는다.
- **노트를 따라 내려갈 때 슬라이드 번호가 뒤로 가면 안 된다.** 읽는 도중에 슬라이드가
  앞뒤로 튀면 따라가는 느낌이 깨진다. `verify.mjs`가 경고로 알려준다.
  경고가 나오면 앵커를 고치는 게 아니라 **노트 순서를 슬라이드에 맞추는 걸 먼저 검토한다.**
  L01의 "분배법칙을 눈으로"와 "De Morgan을 눈으로"가 실제로 이 이유로 자리를 바꿨다.
  (슬라이드가 25쪽 Distributive → 26쪽 De Morgan 순이고, 뒤따르는 예제도 같은 순서였다.)
- 본문에 이미 쓰던 "슬라이드 14~15쪽" 같은 표현은 그대로 둔다. 사람이 읽는 문장이고
  `data-slide`는 기계가 읽는 값이라 역할이 다르다. 다만 **값이 어긋나면 안 된다.**
  본문 표현도 PDF 쪽 기준으로 통일한다.

### 동기화 동작

**동기화는 구간 경계를 넘을 때만 일어난다.** 이게 이 리더의 핵심 규칙이다.

한 주제가 슬라이드 여러 장에 걸치면(`data-slide="12-17"`) 그 **구간 안에서는 노트도
슬라이드도 자유롭게 움직여야 한다.** 스크롤할 때마다 구간의 첫 쪽으로 되돌리면
슬라이드를 읽을 수가 없다. 상태 기반(매번 맞추기)이 아니라 경계 기반(바뀔 때만)이다.

- "지금 어느 구간인가"를 **하나의 상태**로 두고, 그 값이 **바뀔 때만** 반대쪽을 움직인다.
  쪽 번호를 비교하지 않는다. 쪽 번호로 비교하면 사용자가 구간 안에서 슬라이드를
  옮긴 순간 "어긋났다"고 판단해서 매번 되돌린다.
- 구간이 바뀌어도 **상대가 이미 새 구간 안에 있으면 건드리지 않는다.**
- **사용자가 직접 옮긴 슬라이드 위치는 존중한다.** 직접 스크롤하거나 ◀▶를 누르면,
  그 쪽을 감싸는 **가장 넓은 구간**을 기억해 둔다. 노트가 그 구역 안에서 움직이는
  동안에는 슬라이드를 되돌리지 않는다. 섹션(12-17)을 읽다가 그 안의 `h3` 구간(14-15)에
  들어갔다고 해서, 사용자가 보고 있던 16쪽을 14쪽으로 끌고 오면 안 된다.
  그 구역을 벗어나면 기억을 지우고 정상 동기화로 돌아간다.
- 슬라이드를 직접 넘겨 **다른 주제로 가면 노트가 따라간다.** 이건 의도된 동작이다.
  그래서 "수동 조작 시 자동 추적 일시 정지" 같은 장치는 두지 않는다.
  구간 안에 머무는 한 아무 일도 일어나지 않으므로 필요가 없다.
- 양방향이므로 **루프 가드가 필수다.** 단, 시간 창으로 막으면 안 된다.
  부드러운 스크롤이 창보다 오래 걸리면 남은 이벤트가 "사용자 조작"으로 오인된다.
  **목표 위치에 도착했는지로 판정하고**, 도달 불가능한 경우(끝까지 스크롤 등)를 위해
  넉넉한 타임아웃만 보조로 둔다.
- 노트를 따라 슬라이드를 옮길 때는 **즉시 이동**한다. 부드럽게 움직이면 스크롤 중에
  목표가 계속 바뀌어 리더가 뒤처지고 도착 판정도 흔들린다.
- 리더는 **접을 수 있어야 한다.** 접힌 상태가 기본값은 아니지만, 노트만 보고 싶을 때
  방해가 되면 안 된다. `따라가기` 토글로 동기화 자체를 끌 수도 있어야 한다.

### 기준선 바늘

"구간이 바뀌었다"를 판정하는 기준선은 화면 위에서 28% 지점이다. 이게 코드 안에만
있으면 **왜 지금 슬라이드가 넘어갔는지 알 수가 없다.** 그래서 화면에 보이게 한다.

- 본문과 리더의 경계에 왼쪽을 가리키는 작은 삼각형과 옅은 눈금선을 둔다.
  제목 옆 출처 라벨의 선이 이 바늘을 넘어가는 순간이 곧 동기화 시점이다.
- **드래그로 옮길 수 있다.** 끄는 동안 눈금선이 본문 전체로 늘어나 어디에 맞추는지
  보이고, 동기화도 실시간으로 다시 판정된다. 바늘이 제목을 지나는 순간 슬라이드가
  넘어가는 게 보여야 이 장치가 무엇인지 설명 없이 전달된다.
- 동기화가 실제로 일어나면 바늘이 잠깐 강조된다. 원인과 결과를 눈으로 잇기 위한 것.
- 위치는 저장하지 않는다. 세션 안에서만 유지된다 (브라우저 저장소 금지 규칙).

**양쪽에 다 있어야 한다.** 슬라이드쪽에도 같은 바늘을 두되 반대 방향을 가리킨다.
다만 바늘만 옮겨오면 반쪽이다. 노트쪽은 제목 옆 라벨이 "무엇이" 그 선을 넘는지
보여주지만, 슬라이드쪽에는 구간을 나타내는 게 원래 아무것도 없다. 그래서 같이 넣는다:

- **구간 시작 표시**: 새 섹션이 시작하는 쪽 위에 선과 노트 섹션 이름(`06 Kolmogorov의 공리`).
  노트가 슬라이드 쪽수를 가리키는 것의 정확한 반대 방향이다.
- **h3 하위 경계**: 이름표 없이 가는 눈금만. 다 이름을 붙이면 페이지 열이 글자로 빽빽해진다.
- **미포함 쪽**: 어떤 앵커에도 안 걸린 슬라이드는 흐리게 하고 `노트에 없음` 배지를 붙인다.
  표지나 삽입 페이지면 정상이지만, 범위를 짧게 잡아 빠뜨린 것이면 스크롤하다 바로 눈에 띈다.
  실제로 L02 s11이 `32-37`이라 38~39쪽이 빠져 있었는데 이 표시가 있었으면 즉시 보였을 것이다.
  `verify.mjs`도 같은 것을 경고로 알려준다.

### 레이아웃

데스크톱 전용이다. **모바일은 지원하지 않는다.**

- 좁은 화면에서는 리더를 DOM에서 숨기고 PDF를 아예 받지 않는다.
  모바일에서 1MB를 받게 하지 않는다.
- 기준 폭은 `1220px`. 그 아래에서는 기존 2단 레이아웃(TOC + 본문)만 남는다.
- **본문 폭이 500px 아래로 내려가면 안 된다.** 리더가 차지하는 만큼 본문이 밀리는데,
  `.wrap`이 TOC 210px + gap 56 + 좌우 여백 48을 이미 쓰고 있어서 다음이 성립한다.

  ```
  본문 폭 = min(1180, 화면폭 − --rdr-w) − 314
  ```

  리더가 520px 고정이던 시절 1100px 화면에서 본문이 **266px**까지 눌렸다.
  모바일 기준(390px)보다 좁아서 표와 도해가 깨진다. 그래서 `--rdr-w`를
  `clamp(400px, calc(100vw - 814px), 520px)`로 둔다. 계단식(media query)으로 하면
  경계에서 본문이 오히려 좁아지는 구간이 생기므로 `clamp`를 쓴다.
  `verify.mjs`가 임계폭에서 본문 폭을 재서 480px 미만이면 실패시킨다.
- 리더가 켜져 있을 때 본문이 눌려서 표나 도해가 깨지면 안 된다.
  `verify.mjs`의 오버플로우 검사가 데스크톱 폭에서도 걸리는지 확인할 것.

### 저작권 표기

슬라이드를 사이트에 싣게 되면 footer 문구를 반드시 같이 고친다.
자세한 내용은 아래 [footer 문구](#footer-문구) 참조.

## 워크플로우

새 모듈 노트를 만들 때:

1. 슬라이드 PDF를 읽는다. 필요하면 `HW*.pdf`도 읽어서 범위를 확인한다.
   **읽으면서 어느 개념이 PDF 몇 쪽인지 기록해둔다.** 3번에서 `data-slide`로 쓴다.
2. Drive 원본을 `slides/L{NN}-{topic}.pdf`로 복사한다. 노트와 stem을 맞춘다.
3. 기존 `L01`/`L02`를 참고해 스타일을 맞춘다.
4. `L{NN}-{topic}.html`을 작성한다. 각 `<section>`에 `data-slide`를 붙인다.
   슬라이드에 없는 보충 내용에는 붙이지 않는다.
5. `node scripts/verify.mjs L{NN}-{topic}.html` 실행. exit code 0이어야 통과다.
   - 콘솔 에러, SVG 텍스트 이탈, 중복 id, 모바일 가로 오버플로우를 검사한다.
   - `shots/` 폴더에 전체 스크린샷 2장과 **도해별 개별 스크린샷** `-fig01.png…`이 생긴다.
   - **도해 스크린샷을 한 장씩 다 열어본다.** 자동 검사가 통과해도 이건 건너뛰지 않는다.
     테두리가 글자를 관통하는 것, 색 대비, 화살표가 엉킨 것은 눈으로만 잡힌다.
6. 슬라이드 리더가 실제로 따라오는지 확인한다. 노트를 위에서 아래로 훑으면서
   슬라이드가 같이 넘어가는지, `data-slide` 값이 실제 PDF 쪽과 맞는지 본다.
   **어긋난 앵커는 있으나 마나가 아니라 적극적으로 해롭다.** 근거를 잘못 가리키기 때문이다.
7. 문제가 있으면 고치고 5번 반복.
8. `index.html`을 갱신한다:
   - 해당 모듈 카드의 `<div class="mod soon">` → `<a class="mod" href="...">`
   - `<span class="status wait">준비 중</span>` → `<span class="status done">읽기</span>`
   - 닫는 `</div>` → `</a>`
   - 태그 목록을 실제 내용에 맞게 갱신
   - 헤더의 "Module N / M 완료" chip 갱신
   - 다음 모듈 카드를 `soon` 상태로 추가
9. 커밋하고 푸시한다. 커밋 메시지는 한 줄, 영어: `Add Module 3: conditional probability`
   **슬라이드 PDF가 노트보다 늦게 들어가면 안 된다.** 노트만 먼저 올라가면
   리더가 404를 받는다. 미리 들어가 있는 건 상관없다 (아무도 참조하지 않으므로).

## 검증

`verify.mjs`는 저장소 루트를 **임시 http 서버로 띄우고** 그 주소로 연다.
`file://`에서는 브라우저가 PDF와 모듈 로드를 막아 리더를 검증할 수 없기 때문이고,
GitHub Pages와 같은 조건으로 맞추기 위해서다.

`scripts/verify.mjs`가 **자동으로 잡는 것** (하나라도 걸리면 exit 1):
- 페이지 로드 시 JS 콘솔 에러
- SVG `<text>`가 부모 viewBox를 벗어나는 경우
- 중복된 DOM id, 해결되지 않는 `url(#...)` 참조
- 모바일(390px) 가로 오버플로우. **`<details>`를 전부 펼친 상태로 잰다.**
  풀이가 접혀 있을 때는 멀쩡하다가 펼치면 터지는 경우가 실제로 있었다.
- 슬라이드 리더가 실제로 떠서 첫 쪽을 렌더하는지
- `data-slide` 값이 PDF 쪽 수를 벗어나는지 (오타 잡기)
- `data-slide` 범위가 서로 겹치는지 (포함 관계는 정상, 부분 겹침만 잡는다)
- 다른 페이지를 가리키는 `href="x.html#id"`의 대상이 실제로 있는지.
  어긋난 링크는 어긋난 슬라이드 앵커와 같은 이유로 해롭다. 근거를 잘못 가리킨다.
- 본문과 목차 링크가 **라이트/다크 양쪽에서** 배경 대비 3:1 이상인지
- 모든 `<details>`가 **양쪽 언어에서 열리는지** (`<summary>`가 2개면 실패)
- 도해 안에서 **글자끼리 겹치는지**. viewBox 밖으로 나가는 것만 보던 것의 보완이다
- 리더가 뜨는 최소 폭에서 본문이 480px 이상 남는지
- 공략 페이지(`HW*.html`)의 문제 번호와 노트가 언급하는 문제 번호가 일치하는지.
  HW 매핑이 노트와 공략 양쪽에 있어서 한쪽만 고치면 조용히 갈라진다

**경고만 하는 것** (exit code에 영향 없음):
- 도해에서 글자가 **다른 그룹의 도형 위에** 있는 곳. 그 도형의 라벨이면 정상이라
  실패로 치지 않는다. 연산자나 캡션이 걸리면 자리가 겹친 것이니 열어볼 것
- 노트를 따라 내려가는데 슬라이드 번호가 뒤로 가는 곳.
  의도한 재배열일 수 있어 실패로 치지 않지만, 대개는 노트 순서를 고치라는 신호다.

`shots/`에 남기는 것:
- `{stem}-desktop.png`, `{stem}-mobile.png` — 전체 페이지
- `{stem}-fig01.png` … — `figure`와 `.play`를 하나씩 잘라낸 것. 풀이를 펼친 상태로 찍는다.

**사람이 봐야만 잡히는 것** (도해 스크린샷을 한 장씩 열어라):
- 테두리나 도형이 글자를 관통해서 안 읽히는 것 (viewBox 안에 있어도 발생한다)
- 화살표가 엉키거나 대응 관계가 헷갈리게 그려진 것
- 색 대비가 부족한 것, 의미 고정 색상 규칙을 어긴 것

**전혀 못 잡는 것**: 설명의 질, 예제 난이도, 비유가 와닿는지.
이건 사용자 피드백이 유일한 신호다. **완성했다고 단정하지 말고 확인을 요청한다.**

### 모바일 오버플로우가 났을 때

원인은 대개 셋 중 하나다. 세 가지 방어는 이미 `L01`/`L02` 스타일 블록에 들어 있으니
스타일을 복사해왔다면 그대로 유지할 것.

| 원인 | 방어 |
|---|---|
| 그리드 트랙이 `1fr`이라 넓은 자식이 레이아웃 전체를 밀어냄 | `grid-template-columns:minmax(0,1fr)` |
| 표의 min-content가 화면보다 넓음 | `@media(max-width:900px){table{display:block;overflow-x:auto}}` |
| 긴 `.m` 수식이 `white-space:nowrap`이라 안 접힘 | `@media(max-width:900px){.m{white-space:normal}}` |

## 커밋하는 것과 안 하는 것

repo는 **Public**이고 GitHub Pages로 서빙된다. 여기 올리는 건 인터넷에 게시하는 것이다.

`.gitignore`:
```
node_modules/
shots/
package-lock.json
*.pdf
!slides/*.pdf
```

`*.pdf`로 전부 막고 `!slides/*.pdf`로 슬라이드만 되살린다.
**Drive 미러에서 아무 PDF나 repo 루트로 복사하지 않는다.** 교재 57MB가 딸려 들어간다.
`slides/`에 넣을 것은 모듈에 대응되는 강의 슬라이드뿐이다.

| 파일 | 커밋 |
|---|---|
| `slides/L{NN}-*.pdf` (강의 슬라이드) | O |
| `vendor/pdf.js/` | O |
| 교재 (Kay), `randomprocJuly14.pdf` | X (용량 + 저작권) |
| `HW*.pdf`, 강의계획서 | X |
| `shots/`, `node_modules/` | X |

커밋 전에 `git status`로 의도하지 않은 PDF가 스테이징됐는지 확인한다.

### footer 문구

슬라이드를 포함하게 되었으므로 **"원본 강의 슬라이드는 포함되어 있지 않습니다"는
더 이상 쓰지 않는다.** 사실이 아닌 문구를 남겨두는 게 안 쓰는 것보다 나쁘다.
대신 각 노트와 `index.html` footer에 이렇게 밝힌다:

- 강의 슬라이드의 저작권은 담당 교수(Nicolò Michelusi)와 ASU에 있다는 것
- 이 사이트는 수강생이 만든 개인 학습 자료이고 공식 강의 자료가 아니라는 것
- 노트 본문은 슬라이드를 재구성하고 보충한 것이라는 것

### 판정선 개수는 양쪽이 맞아야 한다

노트의 판정선(앵커)과 슬라이드의 판정선(구간 표시)은 개수가 대응해야 한다.
다만 기준은 **앵커 개수가 아니라 "서로 다른 시작 쪽" 개수**다.

```
PDF 표시 개수 (라벨 + 눈금) == 앵커의 서로 다른 시작 쪽 개수
```

노트 앵커 둘이 같은 슬라이드 쪽에서 시작하면 PDF에는 그릴 자리가 하나뿐이라
하나로 접힌다. 이건 노트가 슬라이드보다 잘게 쪼개진 지점이라 정상이다.
실제로 L01은 앵커 17개지만 시작 쪽이 15개여서 표시도 15개다.

**단, 섹션 두 개가 같은 쪽에서 시작하면 안 된다.** 라벨을 하나만 그릴 수 있어서
섹션 이름 하나가 조용히 사라진다. `verify.mjs`가 이 경우만 실패로 잡는다.
(섹션 + 그 안의 h3가 같은 쪽에서 시작하는 건 라벨이 이기므로 손실이 없다.)

### 상단 미니 헤더

노트가 길어서 한참 내리면 큰 제목이 안 보인다. 그래서 목차 맨 위에 작은 헤더를 둔다.

- 내용: `← 모듈 목록` 링크 / `MODULE N` / 영문 제목(`Introduction & Set Theory`).
  **제목은 `header.top`의 `h1.title`에 있는 영문 그대로 쓴다.** 번역하거나 줄이지 않는다.
  큰 제목이 사라진 자리를 대신하는 것이므로 같은 문자열이어야 한다.
- **목차(`nav.toc`)가 이미 sticky이므로 그 안에 넣는다.** 별도의 고정 장치를 만들지 않는다.
- `header.top`의 `h1.title`을 IntersectionObserver로 감시해서, 화면에서 사라지면
  `.on`을 붙여 펼친다. 접힌 상태는 `max-height:0` + `visibility:hidden`이라
  탭 순서에서도 빠진다.
- 모바일에서는 목차가 static이라 의미가 없으므로 숨긴다.
- **모듈 목록으로 돌아가는 링크는 헤더와 미니 헤더 양쪽에 있어야 한다.**
  L01에는 원래 `.backlink`가 아예 없어서 목록으로 돌아갈 방법이 없었다.

## 이중 언어 (한국어 / 영어)

한 파일 안에 두 언어를 나란히 두고 보이는 쪽만 남긴다. 좌하단 버튼으로 전환한다.

```html
<p lang="ko">…</p>
<p lang="en">…</p>
<text x="85" y="59" … lang="ko">현실의 질문</text>
<text x="85" y="59" … lang="en">A real question</text>
```

- **도해·앵커·CSS·리더를 공유하므로 두 언어의 구조가 어긋날 수 없다.** 파일이 커지는 게 비용이다.
- SVG 텍스트도 같은 좌표로 두 벌 겹쳐 둔다. 좌표를 공유하니 위치가 어긋날 수 없다.
- **언어 규칙에는 `!important`가 필요하다.** `nav.toc .mini-head a.back` 같은 선택자가
  더 구체적이어서 `display:none`을 되살리는 일이 실제로 있었다.
- **`<details>` 하나에 `<summary>`를 두 벌 넣지 않는다.** 개폐 버튼이 되는 건
  첫 번째 `<summary>` 하나뿐이다. 두 벌을 넣으면 첫 번째가 숨겨지는 언어에서
  누를 것이 사라져 **내용에 영원히 도달할 수 없다.** 실제로 HW1 P8이 영어 모드에서
  안 열렸다. `<summary><span lang="ko">…</span><span lang="en">…</span></summary>`로 쓴다.
  (`.ex`처럼 블록 전체를 두 벌 두는 경우는 `<details>`도 같이 두 벌이라 문제없다.)
- **`data-slide`가 붙은 `h3`를 두 벌로 만들면 앵커가 중복된다.** 숨겨진 쪽은
  `getBoundingClientRect()`가 전부 0이라 리더가 "화면 맨 위"로 오인한다.
  `reader.js`는 보이는 앵커만 위치 기준으로 쓴다.
- JS가 찍는 문자열은 `L(ko, en)` 헬퍼로 처리하고, 언어를 바꾸면 다시 그린다.
  언어 토글이 `langchange` 이벤트를 쏘고, 인터랙티브 도구와 리더가 그걸 듣는다.

### 영어 문체

**번역문이 아니라 처음부터 영어로 쓴 글이어야 한다.** 한국어 어순을 따라가지 않는다.
내용과 교육적 순서는 같게 두되 문장 구조는 영어답게 재구성한다.

- 2인칭 대화체. 축약형(you'll, that's, it's)을 쓴다. 한국어 반말체의 거리감에 대응한다.
- 비유도 영어에 맞게 바꾼다. "도로를 깐다" → "laying the track".
- 소제목은 직역하지 않는다. "statistics가 상위 과목이 아니다" → "Statistics is not probability, level 2".
- em dash 금지는 영어에도 적용한다.
- 수학 용어와 고유명사는 원래대로 영어 유지.

### 검사

번역 누락은 눈으로 못 잡는다. 스크립트로 확인한다.

- 영어 모드로 바꾸고 `lang="ko"` 조상이 없는데 한글이 남은 요소를 찾는다.
- 반대로 한국어 모드에서 `lang="en"` 요소가 보이는지도 확인한다.
- **`<html lang="ko">` 때문에 `closest('[lang="ko"]')`가 모든 요소에서 참이 된다.**
  루트를 제외해야 한다. 이걸 놓쳐서 "누락 0건"이라는 잘못된 결과를 한 번 냈다.
- **SVG 요소에는 `offsetParent`가 없다.** `undefined !== null`이 참이 되어 오탐이 난다.
  가시성은 계산된 `display`로 판정한다.
